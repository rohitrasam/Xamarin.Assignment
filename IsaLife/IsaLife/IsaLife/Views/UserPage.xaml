<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IsaLife.UserPage">

    <StackLayout>
        <Label Text="List of users" HorizontalTextAlignment="Center" FontSize="Large" FontAttributes="Bold"/>
        <!--<Button Text="Show Employee" Command="{Binding ShowEmployeeCommand}"/>-->
        <ListView ItemsSource="{Binding Employees, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <FlexLayout JustifyContent="SpaceBetween" Padding="10, 0" AlignItems="Center">
                            <Label Text="{Binding Id}"/>
                            <Label Text="{Binding FirstName}"/>
                            <Label Text="{Binding LastName}"/>
                            <Label Text="{Binding Gender}"/>
                        </FlexLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>