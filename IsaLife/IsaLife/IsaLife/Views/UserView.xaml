<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:IsaLife"
             xmlns:viewmodel="clr-namespace:IsaLife.ViewModels" 
             x:Class="IsaLife.UserPage">

    <StackLayout>
        <Label Text="List of users" HorizontalTextAlignment="Center" FontSize="Large" FontAttributes="Bold"/>
        <Button Text="show employees" Command="{Binding ShowEmployeeCommand}"/>
        <ListView ItemsSource="{Binding Employees}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <FlexLayout JustifyContent="SpaceBetween" Padding="10, 0" AlignItems="Center">
                            <Label Text="{Binding id}"/>
                            <Label Text="{Binding name}"/>
                            <Label Text="{Binding first_name}"/>
                        </FlexLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>